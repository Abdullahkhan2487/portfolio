# Optional: GitHub personal access token for higher API rate limits (5000 req/hr)
# Without this, the public API allows 60 requests/hour per IP.
# Create a token at: https://github.com/settings/tokens (no scopes needed for public repos)
VITE_GITHUB_TOKEN=
